#include <cstdlib>
#include <um2c.h>

//#include <thrust/sort.h>
//
// void um2_malloc(void ** const p, int const size) { *p = malloc(size); }
//
// void um2_free(void * const p)
//{
//  if (p != nullptr) {
//    free(p);
//  }
//}
//
// void um2_sort(double * begin, double * end) { thrust::sort(begin, end); }
