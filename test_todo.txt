Vector.push_back(&)
Vector.push_back(&&)
Vector.push_back(n, &)
sortPermutation(Vector)
applyPermutation(Vector, Permutation)

common/permute_vector.hpp
  sortPermutation
  applyPermutation

String.starts_with
String.ends_with
String(char const *, uint64_t)

geometry/morton.hpp
  Ensure that mortonSort works in 3D for: 
    (uint32_t, float)
    (uint32_t, double)
    (uint64_t, double)

Quadrilateral.area
  For a quadrilateral with 3 collinear points, is the area still correct?

Quadrilateral.centroid
  For a quadrilateral with 3 collinear points, is the centroid still correct?

RegularGrid.getCellCentroid

MeshFile.compareGeometry
MeshFile.compareTopology
MeshFile.sortElsets

// TriMesh.regularPartition
//   3 by 3 grid. 
//     6  7  8
//     3  4  5
//     0  1  2
//     The cells on the diagonal (0, 4, 8) should contain 0 and 1. 
//     Cell 6 = 1
//     Cell 2 = 0
//     Cells 1, 3, 5, 7 are indeterminate due to floating point error.

Image
  rasterizePoint

API
  initialize (see UM2-C/test/api)
  finalize
